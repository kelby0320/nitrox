# Build artifacts
target/
**/target/

# xtask-managed cache: fetched Limine binaries, assembled disk image.
tools/build-cache/

# Editor / OS noise
*.swp
*.swo
.DS_Store

# Cargo lockfiles in the kernel and userspace workspaces are checked in
# (they produce the OS artifacts we ship). The `tools/` lockfile is not
# — host tooling can drift.
/tools/Cargo.lock
