# Build artifacts
target/
**/target/

# xtask-managed cache: fetched Limine binaries, assembled disk image.
tools/build-cache/

# Editor / OS noise
*.swp
*.swo
.DS_Store

# Cargo lockfiles are checked in for every workspace, `tools/` included since M11
# Part A — see tools/CLAUDE.md, "The lockfile is the pin".
