[workspace]
resolver = "3"
members = ["init","libkern","libheap","libos","libstream","librsproto","libdraw","compositor","libui","liblog","libcrypto","fs-server-ext4","hello","test-harness","eshell","service-mgr","heartbeat","profile-server","logging-service","auth-service","session-mgr","coreutils","nxsh","tty-server","input-server","libinput"]

# `hello` is a freestanding no_std bin with no unwinder; abort on panic.
# Harmless for the std `init`/`libkern` members (xtask never builds them for
# the bare target).
[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
